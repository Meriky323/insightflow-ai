# InsightFlow AI — Cloudflare 迁移与研究能力验收

验收日期：2026-09-24。线上地址：https://insightflow-ai.pages.dev/

公开演示已部署到 Cloudflare Pages，运行独立只读 API，不再依赖 Railway。原 Railway 子域名属于 Railway，不能在 Cloudflare 接管；简历应换成上述地址。

## 这次完成的修复

1. 原有 FastAPI / SQLite / HTML / CSS / JS 架构保留，本地研究能力不迁移到 React。
2. 独立构建公开案例：使用临时数据库，禁用 dotenv，只导出仓库内保存案例。私有研究库和 API 配置不进入部署包。
3. 本地规则回退改为多标签，增加“很薄、发热、磁吸”等中英文标签。它仍是规则，不是通用语义模型。
4. 历史对比 API 与概览统一到相同来源集合，修复口径不一致。
5. 竞品共同提及只作为发现线索。没有直接产品评论时，不计算该型号的差评率；话题按钮可追溯关联证据。
6. 趋势标签改为样本占比升降，避免小样本精选摘要被误读为总体市场加速。
7. 公开 Ask 基于保存证据检索，返回来源编号与链接；无法支持的问题明确提示证据不足。无付费模型调用。
8. 空行动条目从机会看板移除；保留有明确产品、GTM、验证动作的假设。
9. 首屏改为纸色、墨色、陶土色证据线；缩小中文大标题，降低发光与悬浮装饰，保留 reduced-motion 支持。

## 功能审计与真实验证

| 能力 | 实现与实测 | 可靠性 / 失败路径 | 业务价值与结论边界 |
|---|---|---|---|
| Shopping | 本次真实返回 20 个商品 | 按源独立失败，其他源继续 | 价格、供给、定位线索；不是销量 |
| Walmart | 本次 8 个商品；评论 0 条 | 评论标记 partial，保留商品数据 | 评论不可用时不能虚构消费者洞察 |
| YouTube | 1 个视频、20 条评论 | 发现与评论阶段分别记录状态 | 消费者探索性线索，地域保持 GLOBAL |
| Trends | 本次 105 个趋势点 | 无数据时显示空态 | 仅市场内归一化相对变化，不推断 US 是 AU 的几倍 |
| 社区发现 | 本次 0 条 | partial；可用 CSV/JSON 导入补充 | 不是 Reddit 官方完整采集 |
| CSV/JSON | 自动化测试通过去重、过滤、导入及导出 | 无文本/格式错误有返回 | 可接合规社媒、调研、客服导出；需人工检查字段映射 |
| LLM/Sub2API | 原配置缺少 base URL；按原说明临时使用 localhost:8080/v1 后连接被拒绝 | 本地规则回退；失败不伪装成 AI 成功 | 大模型生成本次未通过，不能宣传已验证 |
| 结构化分析 | 32 条一批的 LLM 提取、主题归一、代表证据；离线多标签测试通过 | LLM 失败回退规则，并记录 analysis_mode | 规则对否定、讽刺、跨语言情绪仍弱，不能作为统计真值 |
| Historical compare | 修复来源筛选一致性并测试 | 无基线显示不足 | 精选样本不是随机抽样，delta 是描述性占比 |
| Market comparison | 公开样本阻断 US/AU 偏好比较 | 无同口径来源即阻断 | 同来源也不自动代表同抽样质量，仍需研究设计 |
| Competitors | 商品事实 + 品牌线索 + 证据抽屉 | 无直接评论不计算产品差评率 | 品牌提及不等于型号评价或国家偏好 |
| Opportunities | 有行动链的假设列表 | 缺少行动的不展示空卡片 | 优先验证建议，不是市场规模 / PMF 证明 |
| Ask | Cloudflare 确定性检索，中英文输入；边界与无关问题测试通过 | 明确 evidence insufficient | 不是通用聊天模型，不能证明因果或预测爆款 |
| Export | 线上 PDF、CSV、DOCX 返回 200，文件可读取 | 保存案例预生成 | 是保存结果导出，不实时采集 |
| Public mode | 仅两期案例快照；写入/设置/连接诊断 403，未知研究 404 | 无外部模型/搜索依赖 | 不持有密钥、不消费 SerpAPI/Sub2API |
| X6 case | 独立中英双语编辑型案例页面 | 保留来源与验证边界 | 不是第二套完整 live workspace，不能冒称已做大样本研究 |

本次真实采集合计 **28 个商品、20 条消费者文本、105 个趋势点**，采用本地规则回退。SerpAPI 账户连通与这些数据源实采均已验证；Sub2API 未通过。各源可能受额度、供应商返回结构和平台变化影响，单次成功不保证持续可用。

## 数据来源真实性

内置旗舰案例是仓库已有的 18 条本期、14 条历史**人工来源摘要**，不是逐字评论，也不是 32 位独立消费者。多条摘要可能来自同一讨论，不能把行数当独立样本量。来源 URL 可追溯不等于每一条摘要在本次均已逐字核验；对无法取得原文的来源不宣称重新验证。

X6 官方发布信息已用官方页面交叉核对：https://news.insta360.com/insta360x6/ 与 https://www.insta360.com/us/product/insta360-x6 。社区讨论仍属于探索性来源，不代表整个消费群体。

Reddit：公开案例摘要 + 社区搜索发现 + 导入，无直连完整 Reddit API。TikTok / Instagram：仅合规导出导入，无实时官方连接器。暂不建设依赖登录态或暴力抓取的脆弱爬虫。

## 验证记录

- Python：33 tests passed（含原始 28 项及多标签、历史口径、竞品归因回归）。
- Cloudflare Worker：4 组测试通过，覆盖公开权限、筛选分页、Ask 不足与引用、对比与导出路由。
- 正式站：24/24 HTTP 验证通过；测试明细另附 JSON。
- 已实际打开首页、中英文切换、旗舰工作台、趋势、证据抽屉、消费者列表、竞品、机会、Ask、PDF 入口。
- 首页已在 1920、1440、1366、1024、768、430、390、375 宽度读取布局；375 手机工作台做过截图检查。
- 不把以上范围夸大为“全部页面、所有按钮、所有分辨率的每种状态均人工点测”。完整本地 LLM 流程仍被未启动的 Sub2API 阻断。

## 设计参考与自评

本次阅读参考：[Linear](https://linear.app/) 的任务层级和先展示工作内容；[Raycast](https://www.raycast.com/) 的快速入口；[Geist](https://vercel.com/geist/introduction) 的层级与一致性。ReactBits 页面可访问但没有提取到组件正文，MotionSites 未取得有效内容，因此不声称复用了这些库或完成了全站视觉调研。

原创调整是纸色 / 墨色 / 陶土色研究桌体系、现有 Evidence Thread 的配色和来源诚实标识、中文标题节奏及研究边界交互。既有项目已包含证据线结构，本次不是把全部设计重新声称为原创。

主观审美自评 **7.5/10**：有清晰辨识度，阅读比旧版稳定；消费者页面仍偏表格，部分中文技术术语和移动端信息密度还可以打磨。不宣称已经达到用户要求的所有视觉重构目标。

## 简历项目文案

**InsightFlow AI｜海外消费者洞察与 GTM 决策工具**

围绕海外新品研究，将商品、消费者反馈与搜索趋势统一为可追溯证据，构建“证据—洞察—产品动作—GTM 动作—验证计划”流程；实现多标签分析、历史口径对齐与跨市场结论限制。采用公开案例演示与本地研究分离架构，迁移至 Cloudflare；单次实测采集 28 个商品、20 条消费者文本及 105 个趋势点，并记录部分数据源失败，避免把不完整覆盖包装成确定结论。

不要写成“AI 准确预测爆款”“提高转化率 xx%”或“Sub2API 已完整验收”。没有真实业务实验支持这些结果。

## 90 秒演示路径

- 0–15 秒：首页说明业务问题：碎片信号如何转化为新品与 GTM 决策。
- 15–35 秒：打开旗舰案例，展示建议决策、产品动作和下一步验证。
- 35–50 秒：趋势话题 → Evidence Thread，说明精选摘要、GLOBAL、原始来源。
- 50–65 秒：展示 US/AU 比较被阻断，以及品牌共同提及不等于型号差评。
- 65–80 秒：Ask 提问“哪些结论目前不能说？”，演示不足与边界。
- 80–90 秒：导出 PDF，说明公开站不消耗密钥，本地连接器可开展新研究，但模型网关需单独运行。

当前可把线上地址作为**公开作品演示**使用；尚不能将完整本地 AI 研究链路标记为全部验收完成。
