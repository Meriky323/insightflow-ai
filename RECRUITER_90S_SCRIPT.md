# InsightFlow AI — 90 秒面试演示路径

## 0–12 秒：一句话定义问题
打开首页 → `Open flagship demo`。

你说：

> 海外新品调研的问题不是缺信息，而是 Reddit、商品页、评论和趋势信号太碎，而且不同国家的数据经常不可比。我做 InsightFlow 是为了把这些真实信号转成可追溯的产品和 GTM 决策。

## 12–30 秒：Overview
指向 3 个 Executive Takeaways。

你说：

> 这里不是先给 Dashboard，而是先告诉业务方三件最值得知道的事。每一个结论都可以继续下钻到真实 evidence，并且系统会显示 confidence，而不是让 GPT 把小样本包装成确定结论。

## 30–46 秒：Trend Radar
切到 `Trend Radar`。

你说：

> 趋势不是让 AI 猜热点。我保存历史 research，并且只用两次研究共同存在的数据源做 topic delta。比如这个 demo 里 compatibility / fit 和 brand trust 在近期样本里更突出。Google Trends 如果没有真实数据，我宁愿显示缺失，也不会生成一条假曲线。

## 46–62 秒：Consumer Voice → Competitive Benchmark
先点一条 Consumer Voice 的来源，再切 Benchmark。

你说：

> 原始证据一直保留。结构化层把文本拆成 topic、driver、barrier、scenario、purchase impact。竞品页再把商品事实和可关联消费者声音结合，而不是把 review count 当销量。

## 62–77 秒：Market Comparison
切到 `Market Comparison`。

你说：

> 这个页面最能体现我的 data sense。现在 US/AU 有商品覆盖，但 consumer voice 主要是 GLOBAL Reddit，所以系统直接阻止跨市场偏好比较。真实工作里，这比硬给一个漂亮的 US vs AU 图更重要。

## 77–90 秒：Opportunity Board
切到 `Opportunity Board`。

你说：

> 最后每个机会不是停在“用户抱怨发热”，而是变成 Insight、Product Action、GTM Action 和 Next Validation。我的目标不是做一个爬虫，而是模拟海外业务团队真正的 Evidence → Decision → Action 工作流。

## 面试官继续追问时
- 问“AI 用在哪里”：讲 topic/aspect extraction、grounded evidence、Sub2API、分层抽样，不要只说“接了 GPT”。
- 问“为什么没有 TikTok/Instagram”：讲 connector 与 analysis 解耦，先保证证据与决策链路，CSV/JSON 可接 social-listening export，避免为了数据源数量牺牲可靠性。
- 问“怎么验证”：讲 physical benchmark、concept test、价格-feature matrix、matched market sources。
